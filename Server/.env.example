# Backend Environment Variables
# Copy this file to .env and fill in your values
# IMPORTANT: .env should be in .gitignore (not committed to git)

# Server Port
PORT=2068

# MongoDB Connection
MONGODB_URI=your_mongodb_connection_string

# JWT Secret (use a strong random string)
JWT_SECRET=your_super_secret_jwt_key_here

# Node Environment
NODE_ENV=production

# Frontend URLs for CORS (separate multiple with commas)
# Development: http://localhost:5173, http://localhost:3000
# Production: https://your-vercel-url.com
FRONTEND_URL=https://dev-scroll-blog.vercel.app

# Render.com specific (if deploying on Render)
# Render automatically provides RENDER_EXTERNAL_URL
# RENDER_EXTERNAL_URL is used for dynamic URL construction
